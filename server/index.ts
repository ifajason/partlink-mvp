/**
 * @deprecated
 * 本檔案已廢棄。生產環境的 server entry 是 server/_core/index.ts
 * （見 package.json 的 "build" 與 "start" 指令）。
 *
 * 本檔案曾包含硬編碼的 LINE Channel Secret，已在 2026-04-27 移除並廢棄。
 * 保留空殼僅為避免誤匯入；任何修改請改至 server/_core/index.ts。
 *
 * 預計在下次清理時刪除整個檔案。
 */
export {};
